# Changelog

## 1.0 - 2026-08-04

- 优化筛选栏下拉菜单：增加圆角、轻描边与选中项底色，统一勾选标记和文字对齐，并将菜单向下偏移 6 dp，避免紧贴筛选框。
- 会话页顶部继续作为唯一的 Profile 切换入口，移除筛选栏中重复且不可操作的 Profile 标签。
- 筛选栏精简为“最近 / 项目”模式、项目范围和时间范围三个下拉按钮，并重新分配宽度以减少截断。
- 汇总 v0.8 的草稿恢复、发送重试、防闪退、头像裁剪、Profile 隔离和会话筛选等稳定功能，发布为 v1.0 测试基线。

## 0.8.1 - 2026-08-04

- “跳到底部”改为 40 dp 圆形向下箭头按钮，移除文字；长消息会继续滚动到最后一条内容的末尾，不再停在消息开头。
- 将“最近 / 项目”从独立分段栏合并到筛选栏，改为单个下拉按钮，首页减少一行占用。
- 修复头像裁剪中左右、上下滑块只改变数值但预览画面不移动的问题；缩放后的预览会按滑块方向实时平移，最终裁剪范围保持一致。
- Profile 说明字段只接受真实字符串；服务端返回的布尔值 `false`、`true` 或 `null` 不再显示在 Profile 菜单中。

## 0.8 - 2026-08-04

- 每个会话自动保存未发送文字草稿，并按 `Profile + session id` 隔离；退出聊天、切换 Profile 或重启 APP 后重新进入仍会恢复。
- 聊天离开底部后显示“跳到底部”，流式回复只在用户仍靠近底部时自动跟随，不抢夺正在阅读的滚动位置。
- 消息发送失败会恢复文字和附件，并显示“一键重新发送”；空的乐观消息会从聊天记录移除，避免重试时重复显示。
- 会话首页增加当前 Profile、项目和时间筛选；时间支持全部、今天、近 7 天和近 30 天，Profile 切换状态更明确。
- 用户头像与 Hermes 头像在保存前增加方形裁剪，可调整缩放、左右和上下位置；最终图片仍只写入应用私有目录。
- 加强启动防闪退：本机设置读取增加坏数据兜底，服务端返回空 Profile 列表时安全回退 `default`，已删除的当前 Profile 会自动切换到可用项。
- 本轮暂不增加消息正文全文搜索，也不增加长回复工具/推理过程折叠。

## 0.6.5.12 - 2026-08-04

- 恢复用户头像和 Hermes 头像的独立选择、更换与恢复默认功能，选择入口使用 `ACTION_OPEN_DOCUMENT`。
- 所选图片会在后台校验、按最长边 1024 像素缩放并重新编码到应用私有目录；配置不再长期保存相册临时 URI。
- 启动时校验头像只能来自 `filesDir/avatars`，历史 `content://` 地址、越界文件或已删除文件会自动清空并回退内置头像。
- 头像读取失败和授权失效只显示错误提示，不再进入 Android `ImageView` 主线程测量链路，修复 OnePlus / Android 16 启动闪退。
- 保留 0.6.5.11 的 Profile 切换、HTTP/WebSocket Profile 参数和分 Profile 缓存隔离。
- 新增私有头像 URI 白名单测试，并继续通过现有 Profile、会话、Markdown 与流式恢复测试。

## 0.6.5.11 - 2026-08-03

- 在会话页 Hermes 名称下增加 Profile 切换菜单，读取 `/api/profiles` 并显示说明、模型等辅助信息。
- 当前 Profile 保存到本机，重新登录或重启后自动恢复；服务端不支持 Profile 列表接口时回退到 `default`，兼容旧网关。
- HTTP API 自动附加 `profile` 查询参数，WebSocket JSON-RPC 自动携带 `profile`，覆盖会话、项目、设置、Skills、MCP 与 Cron 等请求。
- 会话消息缓存和未读标记使用 `Profile + session id` 复合键，避免不同 Profile 的同名会话互相污染。
- 回复生成期间阻止切换 Profile；切换时清空页面级缓存，并丢弃旧 Profile 的迟到会话/项目响应。
- 新增 Profile URL 编码、显式参数保留和 Profile 列表解析单元测试。

## 0.6.5.10 - 2026-08-03

- 将聊天消息中的 `@image:/绝对路径/图片.png` 识别为内联图片，而不是继续显示为普通路径文字；同时兼容标准 Markdown 图片语法。
- 内联图片由远程网关异步读取并按当前会话缓存，加载完成后直接显示真实缩略图；点击缩略图可进入现有的全屏缩放预览。
- 缩略图读取失败不会影响聊天消息与页面稳定性，同一路径在当前会话中不会反复请求。
- 从聊天打开 Markdown 文档时记录来源页面，点击顶部返回箭头或使用 Android 系统返回键都会直接回到原对话，不再停留在空间页。
- 从聊天打开图片仍使用覆盖式全屏预览，关闭按钮和系统返回键均回到当前对话。
- 新增 Hermes 图片引用解析单元测试，覆盖 `@image:`、标准 Markdown 图片与多图片消息。

## 0.6.5.9 - 2026-08-03

- 对话列表页 Hermes 昵称下方的状态文案由“个人助理 · 网关在线”精简为“网关在线”。
- 将聊天消息中的 `MEDIA:/...md`、整行 Markdown 文档链接以及正文中的绝对 `.md/.markdown` 路径识别为独立文档卡片，扩大为整卡点击区域；点击后直接读取远程文件并跳转 APP 内 Markdown 预览。
- 文档链接解析加入前后段落分块处理，修复文件路径与说明文字被合并为普通段落、视觉上像链接但难以触发的问题。
- Markdown 表格行改为基于最高单元格统一等高，修复长文本所在列增高后另一列边框提前结束、出现断层和空白格的问题。
- 一至两列表格自动铺满正文可用宽度，三列以上保持合理最小列宽并支持横向滚动。

## 0.6.5.8 - 2026-08-03

- 用户头像固定改为随 APP 内置的竹林背景卡通人物图片，统一用于“我的”主页、个人资料、用户聊天消息等全部位置。
- Hermes 头像继续固定使用当前内置 Hermes 形象；移除用户与 Hermes 的“选择头像 / 恢复默认”入口以及系统照片选择、私有文件转换和外部 URI 读取代码。
- 所有历史自定义头像 URI 均被忽略，保存个人资料时会自动清空头像路径，从根源上消除头像授权、解码和持久化问题。
- 个人资料页只保留用户昵称、签名、网关账号和 Hermes 昵称设置，并明确标注两枚头像均为 APP 内置资源。

## 0.6.5.7 - 2026-08-03

- 修复头像已提示选择成功、草稿中也出现“恢复默认”，但预览和保存后仍显示昵称首字母的问题。
- 应用私有头像不再转换成 `file://` URI 后交给厂商 `ContentResolver`，改为直接读取应用私有文件路径；同时兼容 0.6.5.6 已保存的旧 `file://` 路径。
- 头像导入完成后增加二次解码校验：只有生成的 PNG 能被实际头像读取器识别时才提示选择成功，避免假成功。

## 0.6.5.6 - 2026-08-03

- 用户与 Hermes 头像改用 Android 系统照片选择器；选中后立即读取、校验、自动缩放并重新编码为应用私有 PNG，兼容相册临时授权、HEIC/特殊扩展名和超大原图，并显示明确的处理中、成功或失败提示。
- 全部页面返回箭头统一改为单色中性灰，移除原先灰蓝拼色；深色模式自动切换为高可读浅灰。
- 聊天正文中的 `MEDIA:/...` 文件地址和标准 Markdown 链接改为可点击样式；Markdown 文件直接跳转 APP 内预览编辑，图片进入图片预览，HTTP/HTTPS 链接交给浏览器打开。
- 新增聊天文件链接规范化测试，兼容大小写 `MEDIA:` 前缀、反引号包裹路径与中文文件名。

## 0.6.5.5 - 2026-08-03

- 移除头像组件中的 `AndroidView/ImageView` URI 懒加载链路，改为在后台真正打开数据流并安全解码为 Compose 位图；相册授权失效、文件不存在或图片损坏时直接回退默认头像，不再在主线程测量阶段闪退。
- 兼容 v0.6.5.4 以前已保存的临时相册 URI：无需清除数据即可重新进入应用，之后在个人资料中重新选择头像便会迁移到应用私有目录。
- 将桌面自适应图标主体由画布约 66% 进一步缩至约 53%，为 OnePlus 等会额外放大前景层的启动器保留更明显的四周留白。

## 0.6.5.4 - 2026-08-03

- 修复 Android 16/OnePlus 在应用重启后读取已失效系统相册 URI 时触发 `SecurityException` 并闪退的问题；头像加载失败会安全回退，不再中断主界面。
- 新选择的用户与 Hermes 头像会立即复制到应用私有目录，配置中不再保存依赖系统临时授权的相册 URI，从根源上避免授权被回收。
- Hermes 自定义头像不可访问时统一回退为内置 Hermes 图标；旧版本保存的失效头像只需在个人资料中重新选择一次即可迁移。
- 进一步收缩桌面自适应图标前景安全区；即使厂商桌面对前景层额外放大，头发、耳机和肩部仍保持完整。

## 0.6.5.3 - 2026-08-03

- 应用图标改为确认后的轻量扁平 Hermes 形象：白色底、浅蓝不规则装饰衬底、棕色头发与青色耳机，并轻微降低人物肤色暖度。
- 桌面启动图标、圆形/圆角自适应图标、应用内 Hermes 标志与高分辨率备用图同步替换为同一视觉方案。
- 自适应启动器前景单独加入安全边距，兼顾图标饱满度并避免不同厂商桌面裁切头发、耳机或肩部。

## 0.6.5.2 - 2026-08-03

- 根据新的棕发耳机 Hermes 人物图重新设计应用图标：清理白边，升级为深蓝渐变背景与青色轮廓光，并为自适应图标单独保留安全区构图。
- 同步更新 Android 13 主题图标为人物与耳机轮廓，避免启用系统主题图标时仍显示旧的双翼权杖图案。
- 对话列表左上角优先显示个人资料中上传的 Hermes 头像；未设置时回退为内置应用图。
- 收紧 Hermes 昵称和网关在线状态之间的垂直间距，同时保持两行区域与头像等高。

## 0.6.5.1 - 2026-08-02

- 放大底栏四枚图标并缩小标签文字；对话存在未读回复时，底栏“对话”图标同步显示红点角标。
- 对话列表页 Hermes 头像由 32 dp 放大至 42 dp，昵称字号收紧，昵称与网关状态两行总高度和头像一致。
- Android 12 及以上保留系统强制启动窗口，但移除居中的应用图标并将背景匹配应用明暗主题；较早系统的启动背景也改为应用首屏颜色。

## 0.6.5 - 2026-08-02

- 对话流式请求与当前页面解耦：返回会话列表后继续生成，完成时按通知设置发送系统提醒，并为未查看的会话显示持久化未读红点。
- 个人资料增加 Hermes 专属头像与昵称设置，仅用于聊天气泡、聊天标题区与助理面板；用户头像主卡外框统一为圆角矩形。
- 底栏激活图标保持完整彩色，未激活图标改为保留明度层次的灰度效果。
- 微调“我的”底栏图标的头身比例；移除定时任务时钟左上角的浅绿色回转装饰，改为更清晰的完整钟面。
- 将普通页面加载失败与流式请求失败分开处理，避免会话列表刷新异常意外终止后台回复。

## 0.6.4.4 - 2026-08-02

- 修复自 v0.6.3 引入的确定性启动崩溃：`HermesMark` 曾通过 Compose `painterResource` 直接加载 `layer-list`，而 Compose 仅支持位图与 VectorDrawable。
- 应用内 Hermes 标志改为直接加载 PNG 应用图；旧 `hermes_logo_tile` 同时重写为真正的 VectorDrawable，杜绝再次误用 LayerDrawable。
- 扫描全部 Compose 图片入口，确认其余 79 枚 Hermes Light 图标均为 PNG，没有第二个不受支持的 drawable 类型。

## 0.6.4.3 - 2026-08-02

- 将真机 Debug 安装包恢复为 R8 单 DEX，避免部分厂商系统在全新安装后加载 11 个 DEX 时于首帧前终止进程；继续关闭资源裁剪以保留完整图标资源。
- 崩溃捕获提前到高优先级 ContentProvider，在 Application 和 Activity 创建前安装，尽可能记录启动早期异常。
- 保留 v0.6.4.2 的重复会话 ID 去重、安全启动与通知初始化隔离修复。

## 0.6.4.2 - 2026-08-02

- 修复自动恢复登录后会话列表可能因重复会话 ID 触发 Compose 重复键异常并直接退出的问题；进入 UI 前统一对会话数据去重。
- 新增真机崩溃诊断与安全启动：致命异常会保存设备、系统和堆栈信息；下次启动暂停自动登录并提供可复制报告，避免无提示连续闪退。
- 通知渠道和定时轮询初始化增加故障隔离，个别厂商系统的通知服务异常不再影响主界面启动。

## 0.6.4.1 - 2026-08-02

- 新增真机兼容修复：79 枚 Hermes Light 图标改用日/夜双套 PNG 资源，避开部分系统在运行时解析复杂 VectorDrawable 时的兼容风险。
- 应用启动图改为直接 PNG 前景，移除嵌套图层和 WebP 解码链路，同时继续保留自适应图标安全区。
- Debug 安装包关闭 R8 与资源压缩，便于稳定试装和后续获取准确崩溃信息；版本号提升至 26，可直接覆盖 v0.6.4。

## 0.6.4 - 2026-08-02

- 将用户确认的 79 枚 Hermes Light 图标完整接入 Compose 页面与弹层，覆盖底栏、会话、空间、任务、设置、Markdown、状态和全部基础操作。
- 移除 Material 扩展图标依赖；返回、关闭、发送、刷新、编辑、展开、复制、待办状态和拖动条统一使用同一套 24 × 24 原生 VectorDrawable。
- 图标色板升级为蓝、青、绿、紫、黄、红与中性色的日/夜双资源，深色模式下的文字型与线性图标自动提亮。
- 状态圆点替换为连接、忙碌、错误三类正式状态图标；彩色发送与停止按钮使用浅色语义容器，避免图标与背景混色。
- 应用启动图标替换为重新构图的 Hermes 女生形象，使用全幅自适应前景并保留系统圆形、圆角矩形与异形蒙版的安全区域。
- 新增 SVG 到 Android VectorDrawable 的可重复生成脚本，确保 79 枚资源名称、颜色和几何规则可持续维护。

## 0.6.3 - 2026-08-02

- 以底栏「对话」C 方案为母版，重绘 29 个应用语义图标；统一 24 × 24 网格、圆润实心轮廓、视觉占比和小尺寸识别力。
- 移除图标中的无功能圆点、装饰角标和随机色块，仅保留通知铃舌、图片太阳、文件折角等有语义的细节。
- 对话、空间、任务、我的四个底栏图标，以及 AI、模型、项目、文件、附件、麦克风等重点入口完成单独精修。
- 应用启动图标、应用内 Hermes 标志和系统通知小图标同步升级；深色模式继续使用独立高明度色板。
- 清理旧版多色资源，并将语音语言弹窗中的文字勾选符替换为正式圆角矢量图标。

## 0.6.2 - 2026-08-02

- “我的”顶部头像、昵称与签名升级为固定个人资料主卡，设置列表滚动时保持可见；移除与主卡点击功能重复的“个人资料”设置行。
- 会话首页“最近 / 项目”分段栏恢复整行宽度与居中布局，同时保留较 0.6.0 更紧凑的文字、控件高度与圆角。
- 会话标题由 16 sp 收紧为 14 sp，并增加列表行距、摘要间距及列表前后留白，缓解信息拥挤。
- 任务页刷新与新建入口并入任务概览卡底部操作区，移除孤立的右上角按钮组，使统计、操作与分段栏形成连续布局。

## 0.6.1 - 2026-08-02

- 重校深色主题背景、区块、分割线、正文和辅助文字对比度；多色资源图标及系统状态栏、导航栏图标会随运行时主题正确反色。
- “轻透毛玻璃”调整为“圆润卡片”：保留更丰富的圆角矩形、椭圆控件与轻阴影，面板、顶栏和弹层全部使用不透明实体表面，并移除顶部白色反光层。
- 会话首页“最近 / 项目”改为紧凑分段控件；技能、工具集、MCP 与任务页统一使用同一分段控件。
- 任务概览移除无实际语义的三色圆点，放大数字并与说明文字垂直居中；主任务页移除标题图标，刷新与新建操作改到右上角紧凑操作组。
- 空间、任务和“我的”移除重复顶部标题及图标，内容整体上移；模型、连接、个人资料、通知、语音、高级设置和定时任务详情使用固定顶栏。
- 所有设置和任务开关统一缩放为更小巧的控件尺寸，同时保留整行点击能力。
- 聊天用户消息新增本机头像与昵称；“我的”设置列表新增明确的个人资料入口，未设置头像时显示昵称首字占位。
- Markdown 编辑与预览整体缩小字号，移除底栏上方重复留白；表格统一列宽并支持整表横向滚动，代码复制按钮同步缩小。

## 0.6.0 - 2026-08-02

- 重建全局 UI 基础规范：页面边距统一为 16 dp，圆角收敛为控件、面板、弹层三档，正文和辅助文字恢复适合中文阅读的字号与行高。
- “清爽办公”改为无阴影、少边框的平面内容层；“轻透毛玻璃”使用可透出环境色的半透明表面、柔和阴影、亮边和顶部反光，两个皮肤不再只是透明度差异。
- 新增成功、警告和信息等扩展语义色，移除聊天、任务、快捷面板和助理面板中的页面级硬编码颜色，完善深色模式一致性。
- 对话页取消助手传统气泡，改为接近全宽的文档式正文；用户消息保留紧凑气泡，Markdown、代码块、表格和图片获得更大阅读宽度。
- 聊天输入器加大输入与操作触控区，统一附件、语音、发送、斜杠命令和工具状态的层级与圆角。
- 清爽皮肤底栏改为贴底原生导航面，毛玻璃皮肤保留悬浮玻璃 Dock；四个主入口的结构和选中状态保持一致。
- 会话首页、空间、任务和“我的”统一顶部标题节奏；会话马卡龙底色改用明暗主题语义色。
- 空间文件由独立玻璃卡片改为平铺列表和轻分割线，Markdown 阅读页移除多余外围卡片。
- 任务页三个彩色统计块合并为单一状态摘要；“我的”移除客户端版本统计卡，远程网关和设置入口成为主要层级。
- 设置、网关、个人资料和模型相关页面统一页面边距、行高、输入高度和开关尺寸；多个小型操作补足可触控面积。
- 可视化 Markdown 编辑器在切换明暗主题时同步更新编辑画布颜色，并加大工具栏触控区域。

## 0.5.6 - 2026-08-02

- 聊天输入框键入 `/` 后显示 Hermes 命令面板；空查询读取 `commands.catalog` 分类目录，继续输入则调用 `complete.slash` 实时筛选。
- 斜杠命令列表兼容 Hermes 内置命令、用户快捷命令和已安装技能命令；网关暂不可用时提供常用命令降级列表。
- 工作空间 Markdown 预览补齐一级至六级标题、Setext 标题、分割线、任务列表、删除线、斜体、引用、代码块、表格和图片排版。
- Markdown 预览升级为留白更舒适的文档画布，表格增加表头层次、边框与横向滚动，文档内相对路径图片可直接打开预览。
- Markdown 编辑由源码输入升级为所见即所得编辑器，提供正文、标题、粗体、斜体、项目列表、编号列表、引用和分割线工具栏，保存时仍写回标准 Markdown。

## 0.5.5 - 2026-08-02

- 模型、对话风格、审批、记忆等设置页移除“默认模型、辅助模型、Mixture of Agents”等非必要分组小标题，改为更连续的高密度列表。
- 数值输入框与新增模型提供商表单统一改为浅灰色无描边紧凑输入区，高度接近同行标题文字。
- “我的”顶部个人信息改为独立完整页面，支持设置头像、昵称和个人签名，不再使用底部弹层。
- 远程网关页面重做为与其他设置页一致的标题、状态条、紧凑表单和说明行，并移除重复的外观选择区域。
- 修复部分安卓系统因语音服务能力误判导致“启用语音输入”无法点击的问题；开关不再受检测结果禁用。
- 语音设置增加双重服务检测、识别语言选择和“测试语音识别”入口，便于直接确认手机系统服务状态。
- “我的”和关于页移除非必要区块小标题，进一步增加单屏内容容量。

## 0.5.4 - 2026-08-02

- 定时任务详情移除底部“关闭”文字按钮，立即运行、暂停/恢复、编辑和删除改为紧凑图标操作栏；编辑状态使用取消与保存图标。
- 浅色主题的弹窗、菜单和底部弹层统一使用白色中性表面，不再出现淡粉色底色。
- 模型设置增强默认模型读取兼容，优先显示服务端配置并回退到实时模型状态。
- 上下文窗口改为左侧固定标题、右侧紧凑数值输入框；模型、对话风格、审批、记忆等设置统一为高密度行式布局。
- 缩小设置页开关、图标、分组标题和纵向间距，在保持分组关系的同时增加单屏信息容量。
- “我的”顶部头像区域升级为个人资料入口，支持本机昵称、个人签名与自定义头像，并持久保留图片访问权限。

## 0.5.3 - 2026-08-02

- 新建项目改为浏览 Hermes 服务器目录并选择路径，不再要求在手机上手动输入绝对路径。
- 定时任务详情升级为独立完整页面，长任务内容可完整滚动查看；运行、暂停、编辑、删除和关闭统一放到内容区外的操作栏。
- 首页右上角菜单移除固定宽度，按“新建对话、搜索对话、批量改名”的实际内容紧凑显示。
- “我的”新增“技能与工具”，支持查看技能详情及启停技能、工具集和 MCP 服务。
- 新增服务器模型设置：默认模型、推理强度、8 类辅助模型、MoA、备用模型、上下文窗口和自定义模型提供商。
- 新增对话风格设置：人格、时区和推理过程显示开关。
- 新增审批模式与超时设置，以及持久记忆、用户画像、预算和自动压缩参数。
- 新增已归档对话页面，支持刷新、恢复和永久删除归档会话。

## 0.5.2 - 2026-08-02

- 聊天气泡按内容长度自然收缩，短消息不再占据整行，长消息仍按屏幕宽度自动换行。
- 项目视图支持直接在手机端填写服务器目录并新建 Hermes 项目，新项目可继续用于会话分组和“移至项目”。
- 定时任务卡片与执行记录支持点开详情；可在详情中编辑名称、执行内容和 Cron 计划，并执行、暂停或删除任务。
- “进行中”选项卡同时展示正在运行的对话工具和定时任务。
- “我的”页面会话数量改为读取 `/api/sessions` 返回的真实总数，不再固定显示首批 100 条。
- 移除重复的“连接设置”和无实际管理能力的“附件与存储”，远程网关卡片继续作为唯一连接入口。
- 首页菜单“批量 AI 重命名”精简为“批量改名”。

## 0.5.1 - 2026-08-02

- WebSocket 增加 20 秒心跳保活，降低手机网络与网关回收空闲连接造成的随机断线。
- 流式回复断线后自动重新连接远程网关，并在不重复提交消息的前提下轮询同步服务器端已生成的回答。
- 网络恢复过程中显示中文状态，底层 Socket 英文异常不再直接暴露到界面。
- 断线、停止或失败时自动移除没有内容的助手占位消息，避免聊天中出现空白气泡。
- 自动恢复失败时保留已经收到的部分回答，并提示重新打开当前对话确认服务器结果。

## 0.5.0 - 2026-08-02

- 工作空间图片、聊天消息图片、聊天产物图片和待发送图片附件支持 APP 内全屏预览、双指缩放与拖动。
- 接入 Hermes 官方 `/api/cron/jobs`：支持读取、新建、暂停、恢复、立即执行、删除定时任务，并展示下次执行与最近状态。
- 新增安卓系统通知权限和高优先级消息/任务通知频道，支持弹窗、提示音、振动和桌面角标。
- 使用系统非精确闹钟每 15 分钟补偿检查定时任务状态；应用升级或手机重启后自动恢复检查。
- “我的 → 通知设置”支持细分对话、任务、声音、振动和角标，并可发送测试通知或进入系统频道设置。
- “我的 → 语音输入”支持启停、普通话/粤语/英语识别与识别后自动发送；未安装识别服务时显示不可用状态。
- “关于 Hermes”升级为完整页面，集中展示版本、客户端能力和本机凭据保护方式。

## 0.4.6 - 2026-08-02

- 将 28 个语义图标与置顶标记全面升级为现代填充式 Android VectorDrawable，不再依赖细描边。
- 使用实心轮廓、双色层叠与少量高光增强小尺寸识别度，保持蓝、薄荷绿、琥珀黄、珊瑚红和淡紫的多色语义体系。
- 统一底部导航、首页菜单、会话操作、项目空间、任务、聊天输入区和“我的”页面的图形重量与视觉语言。
- 清爽办公与轻透毛玻璃两套皮肤共用同一套填充式造型，并保留浅色、深色模式的独立色值。
- 补充完整图标总览与资源编译校验，确保全部 VectorDrawable 可被 Android 构建工具正常解析。
- 安装包中的未压缩原生库按 16 KB 页面边界对齐，并保持与 v0.4.5 相同的 v2 签名方案和固定证书。

## 0.4.5 - 2026-08-02

- 移除底部栏和“我的”页面由 Compose Canvas 现场绘制的简化图标，改为正式 Android VectorDrawable 资源。
- 建立 24 × 24 网格、统一线宽、圆角端点和光学留白的 Hermes 图标规范。
- 完成 28 个多色语义图标，覆盖底部导航、会话列表、首页菜单、长按操作、项目空间、任务页、聊天输入区和 AI 助理面板。
- 蓝、薄荷绿、琥珀黄、珊瑚红和淡紫只用于局部点缀；选中状态继续由容器和文字强调，不改变图标自身配色。
- 为深色模式提供独立图标色值；清爽办公和轻透毛玻璃皮肤共用同一图形语言。
- 返回、关闭、发送、展开等系统操作继续使用 Android 原生图标，避免破坏平台操作习惯。
- 黄色置顶标记再次简化，去除多余装饰并保持在会话标题左侧。

## 0.4.4 - 2026-08-02

- 新增 A“清爽办公”和 B“轻透毛玻璃”两套完整皮肤，可在“我的 → 外观与主题”即时切换并持久保存。
- 皮肤与浅色、深色、跟随系统独立设置，可自由组合。
- 首页头像、连接状态、更多菜单及“最近 / 项目”切换区域固定在顶部，滚动时只移动会话列表。
- 重绘黄色折叠丝带式置顶标识，统一放在会话标题左侧，并同步用于长按操作菜单。
- 新增一套 Compose 原生多色矢量图标，底部图标在所有状态保持多色，仅通过背景和文字强调选中项。
- “我的”页面设置、网关、统计等入口统一使用同一多色图标语法。
- 统一全局中性色、蓝、薄荷绿、珊瑚、琥珀和淡紫配色；两套皮肤共享相同颜色规则。
- 清爽皮肤取消主要面板阴影并使用实体表面；毛玻璃皮肤只在顶部、底栏、输入框和弹层使用轻透材质与统一柔和阴影。

## 0.4.3 - 2026-08-02

- 修复聊天加载状态向左偏移；移除白色加载卡背景，改为页面中央的透明轻量状态。
- 恢复输入法避让：聊天输入框随键盘整体上移并紧贴键盘上沿，不再被遮挡，也不保留重复空白。
- 长按会话新增 AI 重命名、置顶/取消置顶、归档、移至项目与删除操作。
- 首页新增“最近 / 项目”视图切换，读取 Hermes 原生项目树并按工作目录分组会话。
- 右上角菜单加入批量 AI 重命名，改为小圆角、无说明副标题的紧凑操作列表。
- 稳定模型选择页高度并禁用底部弹层拖动，避免列表滚动到底时上下抖动。
- 会话列表标题超过 15 个字符时以省略号结尾。
- 空间目录与 Markdown 文档接管系统返回手势，优先返回上级目录或文档列表。

## 0.4.2 - 2026-08-02

- 修复聊天输入框与输入法被重复避让造成的大块空白，统一交由系统 `adjustResize` 处理。
- 聊天流只展示用户和 Hermes 的可读消息；工具与系统原始 JSON、diff、命令结果不再显示为黄色代码气泡。
- 历史消息改为只读取最新 200 条并加入内存缓存，首次进入直接定位到末尾；加载状态改为页面中央的紧凑进度卡。
- 会话列表移除文字首字头像，改用简洁聊天图标和随对话长度变化的半透明马卡龙底色。
- 聊天右上角助理面板新增当前会话模型切换、聊天产物和待办列表。
- 输入框新增 Hermes PC 风格的添加面板，支持文件、图片、链接、项目空间和提示词片段快捷入口。
- 新会话标题复用 Hermes 服务端异步自动总结，移动端自动同步并限制为最多 15 个字符。
- 聊天标题栏显示当前会话模型；模型切换仅作用于当前会话，不修改服务器默认模型。

## 0.4.1 - 2026-08-02

- 参考飞书、钉钉和企业微信的信息流层级，将会话列表改为高密度扁平布局和彩色会话头像。
- 会话第二行显示 Hermes 返回的真实对话摘要，移除“继续上次工作”。
- 修复 Unix 时间戳被错误截断为无意义数字的问题；时间改为“刚刚、x分钟前、x小时前、昨天 HH:mm、M月d日”等中文格式。
- 移除会话行右侧“更多”按钮；长按会话仍可触发删除确认。
- Cron 来源会话默认折叠为“定时任务对话”分组，可按需展开。
- 重做首页右上角快捷菜单，增加统一圆角、边框、彩色图标底和操作说明。
- 外观选择改为毛玻璃底部弹层，并用三张彩色皮肤预览卡切换跟随系统、浅色和深色模式。
- 聊天页改为居中标题、中文时间分隔、左右彩色消息气泡、彩色工具执行卡和上下分层输入框。
- 全局主色调整为蓝、紫、薄荷、珊瑚与琥珀组合；底部四个入口使用独立强调色。
- 保持固定开发签名，可从 v0.3.1–v0.4.0 直接覆盖安装。

## 0.4.0 - 2026-08-02

- 移除首页同步状态、会话数量、版本号、“最近对话”和条数等冗余信息。
- 将新建对话与搜索合并到右上角“更多”菜单，新增独立会话搜索页。
- 首页会话列表支持下拉刷新，减少固定控件占用并提高内容容量。
- 底部导航升级为“对话、空间、任务、我的”四栏。
- 新增服务器项目空间：优先识别 Hermes 当前项目主目录，支持目录浏览与下拉刷新。
- 新增 Markdown 文档 APP 内预览、基础编辑、远程保存与未保存修改提醒。
- “我的”页移除独立外观板块，将外观与主题作为设置行并使用弹窗选择。
- 保持 v0.3.1 起的固定开发签名，可从 v0.3.1/v0.3.2 直接覆盖安装。

## 0.3.2 - 2026-08-02

- 软件显示名称由“Hermes 随行”统一调整为“Hermes”。
- 使用用户提供的角色图制作全新的 Android 自适应应用图标，移除外围白边。
- 重构为更清新的薄荷绿、雾蓝和浅珊瑚毛玻璃视觉，增加环境光晕、半透明表面、细亮边和轻阴影。
- 重新校准全局字号和行高，压缩标题、正文、标签与辅助信息之间的跨度。
- 精简会话首页概览、新建入口、搜索栏、会话卡片与底部导航，提高一屏内容容量。
- 收紧聊天气泡、工具状态、输入栏、任务和个人中心的间距与组件高度。
- 保持远程网关、聊天、附件、语音和固定开发签名不变，可从 v0.3.1 直接覆盖安装。

## 0.3.1 - 2026-08-01

- 移除不会在手机运行时使用的 Compose 调试预览与测试清单依赖，并裁剪未使用的代码、图标和资源，缩减安装包体积。
- 保持 v0.3.0 的界面、远程网关、聊天、附件和语音功能不变。
- v0.3.1 起固定项目开发签名，避免临时构建环境清理后再次造成签名变化；旧版临时签名无法恢复，已安装 v0.2.1/v0.3.0 的设备需先卸载一次。

## 0.3.0 - 2026-08-01

- 按移动办公应用的信息密度重构整体 UI，建立统一配色、圆角、间距、阴影和状态色规范。
- 新增对话、任务、我的三栏悬浮底部导航，聊天与连接页使用独立全屏结构。
- 会话首页新增搜索、连接状态、今日概览和紧凑会话列表。
- 重构聊天消息、工具运行卡片、空白状态与单行复合输入区，提高一屏内容容量。
- 新增任务与运行页面；暂未接入的定时任务和运行历史使用真实空状态，不展示模拟数据。
- 新增个人中心、网关状态、主题切换、附件/语音说明、连接设置和版本信息。
- 重新设计浅色与深色主题，加入分层表面、渐变强调和语义状态色。

## 0.2.1 - 2026-08-01

- 修复 HTTP(S) 到 WebSocket 地址转换错误，恢复新建/继续会话、发送消息、流式回复和停止生成。
- 历史消息改为直接读取 Hermes Sessions API，不再依赖 WebSocket 恢复成功后才显示。
- 语音入口增加系统识别服务检测及异常兜底，避免无可用识别组件的手机闪退。

## 0.2.0 - 2026-08-01

- 连接方式改为 Hermes Desktop 同款远程网关。
- 使用远程 URL、Hermes 用户名和密码登录，移除 API Server Key 配置。
- 实现 Dashboard Cookie 会话、Android Keystore 加密持久化和自动续期。
- 实现 `/api/auth/ws-ticket` 与 `/api/ws` TUI Gateway JSON-RPC 聊天。
- 新建/恢复会话改用 `session.create`、`session.resume` 和 `prompt.submit`。
- 图片改用远程网关 `image.attach_bytes` 上传，停止改用 `session.interrupt`。
- 兼容带路径前缀的远程 URL。
- 允许明确确认后的 HTTP 连接，并增加公网明文传输风险提示。
- 更新阿里云、VPN、HTTPS 反向代理和常见问题说明。

## 0.1.0 - 2026-08-01

- 建立 Jetpack Compose 安卓工程。
- 实现 Hermes 服务器连接与加密配置。
- 实现 Sessions API 会话管理和历史消息。
- 实现 SSE 流式聊天和工具状态。
- 实现停止请求、图片/文本附件、语音输入。
- 实现 Markdown、代码块、表格显示。
- 实现浅色、深色和跟随系统主题。
- 补充产品规划与阿里云服务器接入说明。
